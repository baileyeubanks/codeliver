# Co-Production Pro Reference Mismatch Ledger

Updated: 2026-07-15
Lane: `/Users/baileyeubanks/Desktop/Projects/contentco-op/cco-videopro-definitive-20260715`
Preview: `http://localhost:4113/projects/bp?asset=bp-rodeo-v2&view=review&demo=1`
Baseline preview: `http://localhost:4103/projects/bp?asset=bp-rodeo-v2&view=review&demo=1`

## Current Evidence

- Reopened the saved canonical references at original detail: review cockpit canonical, Co-Deliver cockpit target, lifecycle dashboard, system map, agentic production cycle brand, horizontal lockup, stacked lockup, annotation behavior screenshot, no-regression shell target, compact metric strip reference.
- Reference 9 is unavailable at the supplied Downloads path: `/Users/baileyeubanks/Downloads/f41e61e5-4998-4cfd-8c58-13dbe6490f58-2026-07-15 (1).png`. A matching read-only copy was found and reopened at `/Users/baileyeubanks/.Trash/f41e61e5-4998-4cfd-8c58-13dbe6490f58-2026-07-15 (1).png`; no files were moved or restored.
- `4103` remains the untouched current-product baseline. `4113` is the isolated implementation preview.
- In-app Browser is not exposed in this Codex Desktop runtime (`agent.browsers.list()` returned empty and `agent.browsers.get("iab")` returned `Browser is not available: iab`), so matched screenshot capture and side-by-side browser QA remain blocked. No surface is marked visually clean from screenshots in this ledger.
- Reprobed in-app Browser after the latest passes through the available browser bridge; `agent.browsers.list()` still returns `[]`.
- Latest in-app Browser retry through `node_repl` still reports `available: []` and `Browser is not available: iab`; matched desktop and `390x844` capture is still blocked by the chosen Browser surface.
- Retried the in-app Browser probe after the public review product-lockup pass; it still reports `available: []` and `Browser is not available: iab`.
- Retried the in-app Browser through the required Browser plugin after the front-office intake pass; setup succeeded, `agent.browsers.list()` returned `[]`, and `agent.browsers.get("iab")` still returned `Browser is not available: iab`.
- Retried the in-app Browser through the required Browser plugin after the Studio Systems pass; setup succeeded, `agent.browsers.list()` returned `[]`, and `agent.browsers.get("iab")` still returned `Browser is not available: iab`.
- Latest loop note: reference 9 is still unavailable at the supplied Downloads path during this pass. It was not moved or restored.
- Latest product-identity pass: the 4103 login baseline still exposes `Co-VideoPro` copy, while the isolated 4113 login HTML now exposes `Co-Production Pro`, the supplied `/brand/co-production-pro-horizontal.png` raster, and `Co-Production Pro | Content Co-op` metadata.
- Latest systems pass: `curl -I http://localhost:4113/settings?section=systems&demo=1` returns `200 OK`; `curl -I http://localhost:4113/api/health/live` returns `200 OK`; `curl -I http://localhost:4113/api/health/ready` returns `503 Service Unavailable` with `failedDependencies:["database","storage"]`, so the new Systems surface must remain honest about backend health.
- Latest review-cockpit systems posture pass: `curl -I http://localhost:4113/projects/bp?asset=bp-rodeo-v2&view=review&demo=1` returns `200 OK`, and the isolated preview HTML contains `Studio systems posture`, `Checking readiness`, `Browser online only`, `presence unverified`, and `Not used in demo`. The default review HTML no longer exposes the planned `Screen share` tile; screen-share remains in the Systems settings surface where it is explicitly planned/disabled.
- Latest live-session dock pass: `curl -I http://localhost:4113/projects/bp?asset=bp-rodeo-v2&view=review&demo=1` returns `200 OK`, and the isolated preview HTML contains `Live session`, `Live collaboration readiness`, `Roster only; realtime unverified`, `Comments are record`, `Presence is ephemeral`, `Pin comment`, `Start screen share`, and `Disabled until session contract`.
- Latest source-operations correction pass: `curl -I http://localhost:4113/settings?section=systems&demo=1` returns `200 OK`; `/api/transcode/worker` returns `503 AUTH_NOT_CONFIGURED`, so the Systems UI no longer links to that machine endpoint. Focused tests confirm the Source operations section is read-only, exposes unavailable route state, planned/not-modeled authority, required contracts, and guard copy, and contains no links, buttons, connection handlers, worktree creation controls, or worker-route href.
- Latest annotation-coordinate pass: the review frame-pin handler now measures pointer coordinates against the full video frame rather than the reduced click overlay, clamps pin positions to the rendered frame, and preserves pause-then-focus behavior. This prevents lower-edge pins from drifting into the player controls while keeping the canonical pin/comment-first interaction.
- Latest baseline comparison smoke: the untouched `4103` baseline review route also returns `200 OK`; the `4113` isolated lane preloads `/brand/co-production-pro-horizontal.png` while preserving the same review URL.
- Latest exterior-brand pass: app loading, 404, global error, dashboard redirect, and unavailable-project states now use the supplied stacked Co-Production Pro raster instead of the horizontal cockpit lockup. The project loading skeleton keeps the horizontal cockpit mark, and the mobile navigation drawer now sizes the rendered brand wrapper rather than a non-existent direct `img` child.
- Latest exterior route smokes: `curl -I http://localhost:4113/login?demo=1` returns `200 OK` and preloads both product marks; `curl -I http://localhost:4113/definitely-missing?demo=1` returns `404 Not Found` and preloads `/brand/co-production-pro-stacked.png`. Browser screenshot comparison remains blocked by the unavailable in-app Browser.
- Latest upload terminal-state pass: the demo-visible cockpit upload modal now stays readable after completion or failure, exposes a close control and terminal CTA, and explicitly says when a new version is available in Project Browser and Version history. The production TUS uploader remains unchanged and still exposes pause, resume, retry, quarantine, and refresh-on-completion behavior.
- Latest upload route smokes: `4113` review and versions routes return `200 OK`, while the untouched `4103` review baseline still returns `200 OK`.
- Latest health-identity pass: raw health links keep the stable machine service id `co-deliver` but now expose display identity as `product:"Co-Production Pro"` and `brand:"Content Co-op"`. The isolated `4113` `/api/health` payload reports `port:4113`; `/api/health/ready` remains `503 Service Unavailable` for database/storage instead of pretending production readiness.
- Latest review density pass: tightened the review readiness strip, default systems-posture rail, and multitrack timeline row heights so the player remains the dominant canvas instead of being pushed down by stacked status chrome. Focused cockpit/timeline/toolbar tests passed (30/30), and both `4113` and untouched `4103` review routes returned `200 OK`.
- Latest systems telemetry/mobile pass: the Systems network-quality area no longer renders fixed filled meters for browser-only or planned telemetry. Browser connectivity is labeled as a binary browser signal, upload/WebRTC/packet-loss rows show `Not measured` + `Planned`, and the 390px-relevant Systems readiness/quality/proof grids now stack to one column with readable secondary detail.
- Latest managed-notification pass: non-demo notification settings now render the persisted `NotificationPreferences` component backed by `/api/notifications/preferences` instead of showing disabled local-demo channel controls. Demo mode still uses local notification controls for preview. `curl -I http://localhost:4113/settings?section=notifications&demo=1` returns `200 OK`; non-demo unauthenticated smoke redirects to login, so authenticated visual interaction remains pending.
- Latest demo-session exterior pass: `DemoSessionGuard` now uses the shared bright `exterior-state` loading shell, supplied stacked Co-Production Pro raster, spinner icon, meter, and concise session-return copy instead of the generic dark/background spinner path.
- Final Browser retry for this loop still reports `available browsers: []` and `Browser is not available: iab`; visual side-by-side acceptance remains blocked by the chosen in-app Browser surface.
- Latest in-app Browser retry after the telemetry/mobile pass initialized the bundled browser client, but `agent.browsers.list()` still returned `[]` and `agent.browsers.get("iab")` still returned `Browser is not available: iab`. Browser bootstrap troubleshooting says to report that plainly rather than switching to an unrelated browser backend, so matched desktop and `390x844` screenshot comparison remains blocked.
- Latest in-app Browser retry through the required Browser plugin after the review-cockpit systems posture pass initialized the bundled browser client, but `agent.browsers.list()` still returned `[]` and `agent.browsers.get("iab")` still returned `Browser is not available: iab`. Matched desktop and `390x844` screenshot comparison remains blocked by the unavailable chosen browser.
- Static HTML check: `4113` settings HTML contains `Co-Production Pro` while the untouched `4103` baseline settings HTML still contains `Co-VideoPro`. The systems panel itself is client-rendered, so static HTML is not visual acceptance evidence.
- Runtime smoke: `curl -I http://localhost:4113/projects/bp?asset=bp-rodeo-v2&view=review&demo=1` returns `200 OK`.
- Baseline smoke: `curl -I http://localhost:4103/projects/bp?asset=bp-rodeo-v2&view=review&demo=1` returns `200 OK`.
- Root redirect smoke: `curl -I http://localhost:4113/?demo=1` returns `200 OK`.
- Projects workspace smoke: `curl -I http://localhost:4113/projects?demo=1` returns `200 OK`.
- Library smoke: `curl -I http://localhost:4113/library?demo=1` returns `200 OK`.
- Activity smoke: `curl -I http://localhost:4113/activity?demo=1` returns `200 OK`.
- Review links smoke: `curl -I http://localhost:4113/reviews?demo=1` returns `200 OK`.
- Project intake smoke: `curl -I http://localhost:4113/projects/new?demo=1` returns `200 OK`.
- Settings notification smoke: `curl -I http://localhost:4113/settings?section=notifications&demo=1` returns `200 OK`.
- Not-found route smoke: `curl -I http://localhost:4113/__definitely-missing-route` returns the expected auth redirect to `/login?next=...&demo=1`; authenticated visual verification of `app/not-found.tsx` is still pending browser access.

## Route Inventory

Major pages:

- Dashboard shell: `/`, `/activity`, `/library`, `/projects`, `/projects/new`, `/projects/archive`, `/projects/trash`, `/reviews`, `/settings`.
- Project cockpit: `/projects/[id]` with `asset`, `view=review`, and `surface` state.
- Review/client shells: `/review/[token]`, `/projects/[id]/assets/[assetId]`, `/invite/[token]`.
- Auth shells: `/login`, `/signup`, `/auth/callback`.

Major contracts present:

- Assets, comments, approvals, share links, batch share, transcript, analysis, edit decisions, upload/TUS, transcode, notifications, usage, vault, storage readiness, health, teams, and webhooks.

## Mismatch Ledger

| Priority | Surface | Finding | Status |
| --- | --- | --- | --- |
| P1 | Product identity | Current UI still carried older `Co-VideoPro` customer-facing labels even though the supplied product authority and raster lockups are `Co-Production Pro by Content Co-op`. | Latest pass aligned visible customer-facing shell, login, exterior/loading/error states, cockpit aria/upload copy, public review defaults, share controls, notification/email/export/invite copy, enterprise workspace display, and reusable login theme display copy to `Co-Production Pro`; the legacy theme key now maps to the new display label. Exact supplied rasters remain byte-for-byte intact. |
| P1 | Review hierarchy and density | Canonical references show compact operational readiness chrome and a dominant player/timeline canvas. The current cockpit had accumulated two vertical readiness bands above the player, weakening review focus. | Added compact `cockpit-review-strip` above the review player, then tightened readiness-card height, systems-posture height, and spacing so status remains visible without pushing the player down. It reports status/version, comments, approval, transcript cleanup, and share/batch readiness. Screenshot evidence is still blocked by in-app Browser availability, so this is source/test verified but not visually clean. |
| P1 | Timeline proportions | Canonical timeline carries durable multi-lane density with source, audio, titles/graphics, notes/comments, and cut decisions. Current timeline only rendered populated lanes, often collapsing to source-only, then grew too tall after honest lanes were added. | Added optional honest analysis lanes and tightened row/track heights to preserve dense multitrack proportions without claiming backend waveform/title analysis is complete. Project review now shows source, audio pending, titles pending, comments, and cut decisions. Screenshot evidence is still blocked. |
| P2 | Annotation clarity | Pending pin state existed but was visually too quiet outside the frame marker, and the pin coordinate math could place lower-edge pins too close to the player controls because the click area and render frame used different rectangles. | Added a compact frame-pin chip in the composer, migrated the seek-step preference key to `co-videopro-review-seek-step` with legacy fallback, and corrected frame-pin coordinates to use the full video frame with clamped percentages. Browser screenshot verification remains blocked, so the surface is not marked visually clean. |
| P2 | Annotation lifecycle contract | The enterprise lifecycle contract still advertised a full drawing toolbar surface (`Select, Pen, Arrow, Text, Highlight, Eraser`) even though the intended review behavior is pin/comment-first with cut markers. | Narrowed the lifecycle surface to `Frame pin comments` and made the unavailable full-drawing workflow explicit, so lifecycle/drawer copy no longer implies a completed drawing suite. Pending visual evidence for drawer rendering. |
| P2 | Login/exterior shell | Login shell was bright and compact, but it still read as a form-only surface instead of sharing the operational readiness language used in the cockpit references. | Updated login/AuthShell/signup metadata copy and added a shared access-readiness strip for portal, verified-session/local-demo, and safe return-path authority across login, signup, and invite acceptance. Visual screenshot verification still blocked by browser availability. |
| P2 | Upload/loading states | Upload overlay existed but product copy, queue polish, close control, demo completion/error persistence, and quarantine placement were under-finished. | Updated product copy, added compact overlay queue/close styling, refresh project assets after both released and quarantined TUS completion, and made the `demo=1` cockpit upload terminal states stay readable with close/review CTAs and explicit new-version placement copy. Remaining pass should inspect progress/resume/error/completion visually when browser capture is available. |
| P2 | Approval data contract | Production approval labels need assignee email present in indexed asset records for the cockpit dock and review readiness strip. | Added `assignee_email` to the project asset select list; regression test now passes. |
| P2 | Mobile cockpit | Code has mobile rail/dock and compact review controls, but current matched `390x844` screenshots are blocked by missing in-app Browser. The operator dock tabs also risked squeezing text labels at exactly 390px, and the mobile review shortcut row carried a misleading Transcript action that opened the same review dock as Comments. | Hid text labels again at `max-width: 390px` while preserving icon/count tabs, removed the duplicate mobile Transcript shortcut, changed the mobile review shortcut strip to two distinct actions, and kept the review heading/media selector on one row at 390px. Pending visual evidence. Do not mark clean. |
| P2 | Loading/empty/exterior states | The app-level 404, project loading/empty states, and demo-session redirect fallback had generic or dark-context-adjacent loading moments that were not visibly part of the bright Co-Production Pro product shell. | Added a branded app `loading.tsx`, replaced generic 404 copy with a Co-Production Pro exterior state, changed project-route loading/empty states to a compact bright cockpit skeleton, switched centered exterior/unavailable states to the supplied stacked raster lockup, and updated `DemoSessionGuard` to use the same bright stacked-raster shell while returning users to sign-in. Pending screenshot evidence. |
| P2 | Global error exterior state | The root error fallback still used a dark inline-styled screen, exposed raw error text, and visually broke from the bright Co-Production Pro exterior shell during failure recovery. | Replaced it with the shared bright exterior state, supplied stacked raster brand component, compact recovery copy, reset action, and route-backed projects fallback. Pending screenshot evidence. |
| P2 | Dashboard redirect state | Root dashboard redirect still showed a bare spinner, which broke the polished login/loading/exterior-shell language before the projects route loaded. | Replaced the spinner with the same bright Co-Production Pro exterior loading shell, supplied stacked raster lockup, and concise project-routing copy. Pending screenshot evidence. |
| P2 | Notifications/account shell | Header popovers were too bare for the operational cockpit and lacked explicit routing for preferences, audit context, and empty-state clarity. | Reworked notification popover into compact audit notifications with scoped workspace signal, activity docking, empty state, and `/settings` preferences route. Reworked account menu with role, email/local-session copy, settings, branding/preferences, and sign-out. Pending visual evidence. |
| P2 | Share modal polish | Share controls had a heavier dark overlay and pill-shaped controls that did not match the canonical compact cockpit language. | Kept the existing share contract, added dialog semantics, softened the overlay, squared the modal close and intent icon controls to 6-8px radii, and made intent tiles expose selected state via `aria-pressed`. Pending visual evidence. |
| P2 | Exact 390px mobile density | Mobile dock tabs were guarded, but the top project switcher, header actions, and comment composer still needed explicit narrow-width constraints to prevent crowding at the requested `390x844` viewport. | Added a `max-width: 390px` cockpit guard that tightens project-switcher/header spacing and stacks comment input, timecode, and submit controls in the remaining column. Pending screenshot evidence. |
| P1 | Settings notification preferences | Notification settings were a channel list without an at-a-glance authority/readiness treatment, and managed mode rendered disabled local-demo controls even though a persisted API-backed preference component already existed. | Added a compact notification authority strip, split demo and managed contracts, kept local channel controls only in `demo=1`, and mounted `NotificationPreferences` for managed mode with API-backed persisted per-event preferences. Text still requires a valid E.164 number in demo mode. Authenticated browser interaction and screenshot evidence remain pending. |
| P2 | Media library workspace | The library route still read as a generic asset grid, with no compact operational summary, weak loading/empty states, and less review/approval context than the canonical cockpit family. | Added an asset-management header, compact asset readiness strip, accessible file-type segmented filters, route-backed empty state, and bounded loading skeletons. Pending screenshot evidence. |
| P2 | Production activity workspace | The activity route was a plain feed without cockpit hierarchy, event readiness, filterable review/upload/approval classes, or a route-backed way back into the review workflow. | Added an audit-history header, compact readiness strip, review/upload CTAs, activity filters, honest loading/empty states, and source tests. Pending screenshot evidence. |
| P2 | Review links workspace | The reviews route still behaved like a generic share-link table, without the canonical cockpit's authority framing, readiness strip, or clear distinction between portal creation, delivery, and notification state. | Added a review-authority header, cockpit CTAs, compact share readiness strip, accessible filters, honest empty state, dialog semantics, icon close control, permission labels, and notification-readiness copy. Pending screenshot evidence. |
| P2 | Public review shell identity | Public recipient review loading, error, and default header states still used a generic Content Co-op image path instead of the supplied product lockup, so client review could feel detached from the polished product shell. | Replaced default public review loading/error/header branding with the exact supplied raster product lockup component while preserving custom tenant logo/name/player-label branding for branded links. Pending screenshot evidence. |
| P2 | Brand settings and governance | The Brand tab could edit inherited player/logo values, but it did not show the supplied product lockup as the product identity or summarize review portal/governance/upload readiness in the same compact operational language as the references. | Added a compact brand readiness strip, rendered the exact supplied Co-Production Pro raster lockup in the Brand tab, preserved tenant review branding controls, and made custom logo upload remain explicitly backend-gated until tenant storage and scan contracts exist. Pending screenshot evidence. |
| P1 | Studio systems settings | The Codex-style settings reference and user brief call for a dense capability cockpit covering GitHub repos, SSH/SFTP, plugins, FFmpeg workers, upload readiness, screenshare, live collaboration, network quality, automation hooks, environments/worktrees, permissions, notifications, and system health. The Systems tab also showed fixed meter values for planned network probes, which made unsampled telemetry look measured. | Added a compact `Systems` settings tab with contract-derived readiness, stateful group filters, disabled planned rows, gated route checks, permission/contract copy, no full-access toggle, and health/dependency proof links. Latest pass removed fabricated quality meters: browser state is binary only, upload/WebRTC/packet-loss are `Not measured`/`Planned`, and max-640px Systems readiness/quality/proof grids stack to one column with readable detail for the 390px target. Backend readiness currently returns 503 for database/storage, and screenshot evidence is still blocked. |
| P2 | Health link identity | Systems proof links opened raw JSON that only identified the legacy `co-deliver` service id and hard-coded the original 4103 port, which conflicted with the Co-Production Pro product shell and isolated 4113 lane. | Preserved `co-deliver` as the stable machine id, added `Co-Production Pro` and `Content Co-op` display identity to health/live/ready/dependencies payloads, and made `/api/health` report the current runtime port when available. Readiness remains honest at 503 when database/storage are not configured. |
| P2 | Systems source operations | GitHub repositories, SSH/SFTP remotes, plugins, worktrees, and FFmpeg workers were present as individual capability rows, but the Systems screen did not show one coherent guarded source/media intake workflow. The first source-operations pass also overclaimed sequence and hid route/authority truth behind proof copy. | Corrected the section into a compact read-only contract-state table. It derives icon/status context from existing system capabilities, exposes route state (`No product route`, `No install route`, `Machine API only; no user route`), authority (`Planned`, `Not modeled`, or `media:write + worker token`), required contracts, and explicit no-write/no-secret/worker-readiness guards. It contains no interactive controls and the FFmpeg worker matrix row is disabled instead of linking to `/api/transcode/worker`. Mobile source rows keep state, route, authority, and contract copy visible without clamping. Screenshot evidence is still blocked by in-app Browser availability. |
| P2 | Default review systems posture | Systems, collaboration, network, media-worker, and screenshare posture lived only in Settings, so the default review canvas still under-exposed the robust production cockpit capabilities requested in the long-horizon brief. | Added and refined a compact `Studio systems posture` rail directly under review readiness with a route-backed health probe to `/api/health/ready`, browser-only network signal, listed collaborators with presence explicitly unverified, and media-worker state that says `Not used in demo` or `Credential gated` instead of claiming FFmpeg readiness. Planned screenshare was removed from the default canvas and remains disabled in Systems settings. Screenshot evidence is still blocked by in-app Browser availability. |
| P2 | Review dock live session | The operator dock still treated comments, approvals, transcript, and share readiness as separate concerns without a compact live-session view for networking, screenshare readiness, realtime presence limits, and durable comment authority. | Added a compact `Live session` section inside the existing review dock. It reports listed roster vs unverified realtime presence, browser connection quality, planned/disabled screenshare, and durable comment authority. `Pin comment` focuses the existing comment composer; `Start screen share` is visibly disabled until a session contract exists; `Systems` routes to the Systems settings tab. Pending screenshot evidence. |
| P2 | Front-office business intake | References and the long-horizon product mission describe a production operating system that starts before pre-production with client intake, CRM, quote/proposal, contract/signature, deposit/invoice/payment, and expense readiness; current intake still presented only create/upload/review/deliver. | Added a compact client-intake step, metric-style readiness strip, front-office readiness rows, client/company and business-context inputs, payload persistence, demo activity details, and lifecycle-contract records/capability that keep CRM/proposal/contract/billing/payment/expense authority explicitly planned. Pending screenshot evidence. |
| P2 | Project intake workspace | The new-project route was a basic form detached from the lifecycle model, making workspace creation feel separate from upload, review, and delivery. | Rebuilt the route as a compact project-intake surface with lifecycle setup path, route-backed activity CTA, required-name validation, trimmed payloads, and honest readiness-gated copy for transcript, waveform, notification, and export work. Pending screenshot evidence. |
| P2 | Projects workspace | The main projects route still opened like a generic folder/file browser, with a breadcrumb-first hierarchy, duplicate upload split, unsupported cloud-import affordance, and weak lifecycle/readiness framing. | Reworked the top of `/projects` into a production-library cockpit surface with compact readiness metrics, lifecycle path, route-backed workspace/review/upload actions, honest processing readiness copy, workspace-oriented empty state, and disabled cloud-import copy until connected. Pending screenshot evidence. |
| P2 | Retention workspaces | Archive/trash routes were bare demo collections, with weak restoration hierarchy, no readiness strip, and unclear storage/deletion authority. | Reworked the shared archive/trash collection into a retention workspace with production-library back path, compact readiness metrics, route-backed open-library CTA, polished empty/list states, restore-only affordance, and explicit local-demo/storage-contract copy. Pending screenshot evidence. |
| P3 | Brand lockup asset | Older passes treated `Co-VideoPro` as active copy even though the only supplied product marks are exact `Co-Production Pro` rasters. | Resolved for the current brief: visible product identity now uses Co-Production Pro and the supplied rasters; no redraws or substituted lockups were introduced. |

## Verification

- `node --experimental-strip-types --test tests/co-production-copy.test.ts tests/admin-client-front-door.test.ts tests/enterprise-identity-model.test.ts tests/cockpit-review-timeline.test.ts tests/co-production-brand.test.ts` passed.
- `node --experimental-strip-types --test tests/review-player-policy.test.ts tests/cockpit-control-regressions.test.ts tests/visual-shell-navigation.test.ts tests/journey-upload-sharing.test.ts tests/auth-policy.test.ts` passed after the narrow approval-select fix.
- `node --experimental-strip-types --test tests/journey-upload-sharing.test.ts tests/cockpit-control-regressions.test.ts tests/cockpit-review-timeline.test.ts` passed after the second upload/mobile patch.
- `node --experimental-strip-types --test tests/exterior-states.test.ts tests/admin-client-front-door.test.ts` passed after the exterior/loading/empty-state pass.
- `node --experimental-strip-types --test tests/exterior-states.test.ts tests/activity-product-polish.test.ts tests/library-product-polish.test.ts` passed after the dashboard redirect shell pass.
- `node --experimental-strip-types --test tests/cockpit-popovers.test.ts tests/cockpit-control-regressions.test.ts tests/visual-shell-navigation.test.ts` passed after the notification/account popover pass.
- `node --experimental-strip-types --test tests/journey-upload-sharing.test.ts tests/cockpit-popovers.test.ts` passed after the share modal polish pass.
- `node --experimental-strip-types --test tests/cockpit-control-regressions.test.ts tests/journey-upload-sharing.test.ts` passed after the exact-390px mobile density guard.
- `node --experimental-strip-types --test tests/settings-product-polish.test.ts tests/settings-route.test.ts tests/journey-demo-review.test.ts` passed after the settings notification readiness pass.
- `node --experimental-strip-types --test tests/library-product-polish.test.ts tests/visual-shell-navigation.test.ts tests/journey-demo-review.test.ts` passed after the library workspace pass.
- `node --experimental-strip-types --test tests/activity-product-polish.test.ts tests/library-product-polish.test.ts tests/settings-product-polish.test.ts` passed after the activity workspace pass.
- `node --experimental-strip-types --test tests/reviews-product-polish.test.ts tests/project-intake-polish.test.ts tests/activity-product-polish.test.ts tests/library-product-polish.test.ts tests/exterior-states.test.ts` passed after the review-links and project-intake pass (10 tests passed).
- `node --experimental-strip-types --test tests/projects-workspace-polish.test.ts tests/project-intake-polish.test.ts tests/reviews-product-polish.test.ts tests/library-product-polish.test.ts tests/visual-shell-navigation.test.ts` passed after the projects workspace pass (15 tests passed).
- `node --experimental-strip-types --test tests/retention-workspace-polish.test.ts tests/projects-workspace-polish.test.ts tests/journey-demo-review.test.ts tests/visual-shell-navigation.test.ts` passed after the retention workspace pass (18 tests passed).
- `node --experimental-strip-types --test tests/admin-client-front-door.test.ts tests/auth-policy.test.ts tests/exterior-states.test.ts` passed after the auth access-readiness pass (15 tests passed).
- `node --experimental-strip-types --test tests/exterior-states.test.ts tests/admin-client-front-door.test.ts tests/team-invite-polish.test.ts tests/auth-policy.test.ts` passed after the global error exterior pass (17 tests passed).
- `node --experimental-strip-types --test tests/co-produce-lifecycle-contract.test.ts tests/co-produce-lifecycle-drawer.test.ts tests/cockpit-control-regressions.test.ts tests/public-review-shell-polish.test.ts` passed after the annotation lifecycle contract pass (26 tests passed).
- `node --experimental-strip-types --test tests/project-intake-polish.test.ts tests/co-produce-lifecycle-contract.test.ts tests/co-produce-lifecycle-drawer.test.ts` passed after the front-office intake contract pass (16 tests passed).
- `node --experimental-strip-types --test tests/settings-product-polish.test.ts tests/co-production-brand.test.ts tests/co-production-copy.test.ts tests/admin-client-front-door.test.ts tests/exterior-states.test.ts tests/public-review-shell-polish.test.ts tests/enterprise-identity-model.test.ts tests/journey-upload-sharing.test.ts` passed after the product-identity and Brand-tab readiness pass (37 tests passed).
- `node --experimental-strip-types --test tests/co-production-copy.test.ts tests/settings-product-polish.test.ts tests/admin-client-front-door.test.ts` passed after the reusable login-theme alias pass (13 tests passed).
- `node --experimental-strip-types --test tests/settings-product-polish.test.ts tests/settings-route.test.ts tests/co-produce-lifecycle-contract.test.ts tests/cockpit-navigation-links.test.ts` passed after the Studio Systems settings pass (18 tests passed).
- `npm run typecheck` passed after the Studio Systems settings pass.
- Full `git diff --check` passed after the Studio Systems settings pass.
- Live route smokes after the Studio Systems settings pass: `4113` returns `200 OK` for `/settings?section=systems&demo=1` and `/api/health/live`; `/api/health/ready` returns the expected `503 Service Unavailable` with database and storage failures.
- `node --experimental-strip-types --test tests/cockpit-control-regressions.test.ts` passed after the review-cockpit systems posture pass (12 tests passed).
- `node --experimental-strip-types --test tests/cockpit-control-regressions.test.ts tests/cockpit-review-timeline.test.ts tests/cockpit-toolbar-responsive.test.ts tests/settings-product-polish.test.ts tests/settings-route.test.ts tests/journey-demo-review.test.ts tests/visual-shell-navigation.test.ts` passed after the review-cockpit systems posture pass (43 tests passed).
- `npm run typecheck` passed after the review-cockpit systems posture pass.
- Path-scoped `git diff --check -- components/projects/ProjectCockpit.tsx app/globals.css tests/cockpit-control-regressions.test.ts docs/design-evidence/co-videopro-definitive-20260715/mismatch-ledger.md` passed after the review-cockpit systems posture pass.
- Live route smokes after the review-cockpit systems posture pass: `4113` and untouched `4103` return `200 OK` for `/projects/bp?asset=bp-rodeo-v2&view=review&demo=1`; `4113` liveness returns `200 OK`; `4113` readiness returns the expected `503 Service Unavailable` with database and storage failures.
- `node --experimental-strip-types --test tests/cockpit-control-regressions.test.ts tests/settings-product-polish.test.ts tests/settings-route.test.ts tests/cockpit-toolbar-responsive.test.ts` passed after the sidecar refinement pass (24 tests passed).
- `node --experimental-strip-types --test tests/cockpit-control-regressions.test.ts tests/cockpit-review-timeline.test.ts tests/cockpit-toolbar-responsive.test.ts tests/settings-product-polish.test.ts tests/settings-route.test.ts tests/journey-demo-review.test.ts tests/visual-shell-navigation.test.ts` passed after the sidecar refinement pass (44 tests passed).
- `npm run typecheck` passed after the sidecar refinement pass.
- Path-scoped `git diff --check -- components/projects/ProjectCockpit.tsx components/projects/ProjectCockpit.module.css app/globals.css components/auth/SettingsPillar.module.css tests/cockpit-control-regressions.test.ts tests/settings-product-polish.test.ts docs/design-evidence/co-videopro-definitive-20260715/mismatch-ledger.md` passed after the sidecar refinement pass.
- Live route smokes after the sidecar refinement pass: `4113` and untouched `4103` return `200 OK` for `/projects/bp?asset=bp-rodeo-v2&view=review&demo=1`; `4113` HTML includes `Studio systems posture`, `Checking readiness`, `Browser online only`, `presence unverified`, and `Not used in demo`; `/api/health/ready` still returns the expected `503 Service Unavailable` with database and storage failures.
- `node --experimental-strip-types --test tests/cockpit-control-regressions.test.ts` passed after the live-session dock pass (14 tests passed).
- `node --experimental-strip-types --test tests/cockpit-control-regressions.test.ts tests/cockpit-review-timeline.test.ts tests/cockpit-toolbar-responsive.test.ts tests/settings-product-polish.test.ts tests/settings-route.test.ts tests/journey-demo-review.test.ts tests/visual-shell-navigation.test.ts` passed after the live-session dock pass (45 tests passed).
- `npm run typecheck` passed after the live-session dock pass.
- Path-scoped `git diff --check -- components/projects/ProjectCockpit.tsx app/globals.css tests/cockpit-control-regressions.test.ts docs/design-evidence/co-videopro-definitive-20260715/mismatch-ledger.md` passed after the live-session dock pass.
- Live route smokes after the live-session dock pass: `4113` and untouched `4103` return `200 OK` for `/projects/bp?asset=bp-rodeo-v2&view=review&demo=1`; `4113` HTML includes the live-session dock tokens; `4113` liveness returns `200 OK`; `/api/health/ready` still returns the expected `503 Service Unavailable` with database and storage failures.
- `npm run typecheck` passed after the product-identity and Brand-tab readiness pass.
- Full `git diff --check` passed after the product-identity and Brand-tab readiness pass.
- Live smoke: `curl` returns `200` for `4113` login and settings Brand routes; `4113` login HTML includes Co-Production Pro identity while the untouched `4103` login baseline still includes Co-VideoPro copy.
- `npm run typecheck` passed after the front-office intake contract pass.
- Path-scoped `git diff --check -- 'app/(dashboard)/projects/new/page.tsx' lib/demo/workspace-store.ts lib/co-produce/lifecycle-contract.ts tests/project-intake-polish.test.ts tests/co-produce-lifecycle-contract.test.ts docs/design-evidence/co-videopro-definitive-20260715/mismatch-ledger.md` passed after the front-office intake contract pass.
- Expanded focused regression command passed after the front-office intake contract pass: `node --experimental-strip-types --test tests/activity-product-polish.test.ts tests/admin-client-front-door.test.ts tests/auth-presentation.test.ts tests/co-produce-lifecycle-contract.test.ts tests/co-produce-lifecycle-drawer.test.ts tests/co-production-brand.test.ts tests/co-production-copy.test.ts tests/cockpit-control-regressions.test.ts tests/cockpit-layout.test.ts tests/cockpit-navigation-links.test.ts tests/cockpit-overview-drawer.test.ts tests/cockpit-popovers.test.ts tests/cockpit-review-timeline.test.ts tests/cockpit-toolbar-responsive.test.ts tests/exterior-states.test.ts tests/internal-review-canonical-route.test.ts tests/journey-auth-navigation.test.ts tests/journey-demo-review.test.ts tests/journey-upload-sharing.test.ts tests/library-product-polish.test.ts tests/project-intake-polish.test.ts tests/projects-workspace-polish.test.ts tests/public-recipient-review-shell.test.ts tests/public-review-shell-polish.test.ts tests/retention-workspace-polish.test.ts tests/reviews-product-polish.test.ts tests/settings-product-polish.test.ts tests/team-invite-polish.test.ts tests/transcript-contract.test.ts tests/visual-shell-navigation.test.ts` (128 tests passed).
- Live preview route smokes on `4113` return `200 OK` for `/projects/new?demo=1` and `/projects/bp?asset=bp-rodeo-v2&view=review&demo=1` after the front-office intake contract pass.
- After the readiness-strip micro-pass, `node --experimental-strip-types --test tests/project-intake-polish.test.ts tests/co-produce-lifecycle-contract.test.ts tests/co-produce-lifecycle-drawer.test.ts` passed again (16 tests passed), `npm run typecheck` passed, and the expanded focused regression command passed again (128 tests passed).
- Live preview HTML for `/projects/new?demo=1` includes `Intake readiness`, `Project write`, `Billing authority`, `No payment state claimed`, `Front-office readiness`, `Client / company`, `Business context`, and `CRM, contracts`.
- Expanded focused regression command passed after the annotation lifecycle contract pass: `node --experimental-strip-types --test tests/activity-product-polish.test.ts tests/admin-client-front-door.test.ts tests/auth-presentation.test.ts tests/co-produce-lifecycle-contract.test.ts tests/co-produce-lifecycle-drawer.test.ts tests/co-production-brand.test.ts tests/co-production-copy.test.ts tests/cockpit-control-regressions.test.ts tests/cockpit-layout.test.ts tests/cockpit-navigation-links.test.ts tests/cockpit-overview-drawer.test.ts tests/cockpit-popovers.test.ts tests/cockpit-review-timeline.test.ts tests/cockpit-toolbar-responsive.test.ts tests/exterior-states.test.ts tests/internal-review-canonical-route.test.ts tests/journey-auth-navigation.test.ts tests/journey-demo-review.test.ts tests/journey-upload-sharing.test.ts tests/library-product-polish.test.ts tests/project-intake-polish.test.ts tests/projects-workspace-polish.test.ts tests/public-recipient-review-shell.test.ts tests/public-review-shell-polish.test.ts tests/retention-workspace-polish.test.ts tests/reviews-product-polish.test.ts tests/settings-product-polish.test.ts tests/team-invite-polish.test.ts tests/transcript-contract.test.ts tests/visual-shell-navigation.test.ts` (127 tests passed).
- `npm run typecheck` passed after the annotation lifecycle contract pass.
- Live preview route smoke on `4113` still returns `200 OK` for `/projects/bp?asset=bp-rodeo-v2&view=review&demo=1` after the annotation lifecycle contract pass.
- `node --experimental-strip-types --test tests/public-review-shell-polish.test.ts tests/admin-client-front-door.test.ts tests/journey-demo-review.test.ts` passed after the public review product-lockup pass (16 tests passed).
- Expanded focused regression command passed after the public review product-lockup pass: `node --experimental-strip-types --test tests/activity-product-polish.test.ts tests/admin-client-front-door.test.ts tests/auth-presentation.test.ts tests/co-produce-lifecycle-contract.test.ts tests/co-produce-lifecycle-drawer.test.ts tests/co-production-brand.test.ts tests/co-production-copy.test.ts tests/cockpit-control-regressions.test.ts tests/cockpit-layout.test.ts tests/cockpit-navigation-links.test.ts tests/cockpit-overview-drawer.test.ts tests/cockpit-popovers.test.ts tests/cockpit-review-timeline.test.ts tests/cockpit-toolbar-responsive.test.ts tests/exterior-states.test.ts tests/internal-review-canonical-route.test.ts tests/journey-auth-navigation.test.ts tests/journey-demo-review.test.ts tests/journey-upload-sharing.test.ts tests/library-product-polish.test.ts tests/project-intake-polish.test.ts tests/projects-workspace-polish.test.ts tests/public-recipient-review-shell.test.ts tests/public-review-shell-polish.test.ts tests/retention-workspace-polish.test.ts tests/reviews-product-polish.test.ts tests/settings-product-polish.test.ts tests/team-invite-polish.test.ts tests/transcript-contract.test.ts tests/visual-shell-navigation.test.ts` (126 tests passed).
- `npm run typecheck` passed after the public review product-lockup pass.
- Live preview route smokes on `4113` return `200 OK` for `/review/demo?demo=1` and `/projects/bp?asset=bp-rodeo-v2&view=review&demo=1` after the public review product-lockup pass.
- Live preview HTML for `/review/demo?demo=1` includes the supplied `/brand/co-production-pro-horizontal.png` raster.
- Path-scoped `git diff --check -- components/review/PublicReviewWorkspace.tsx components/review/PublicReviewWorkspace.module.css tests/public-review-shell-polish.test.ts docs/design-evidence/co-videopro-definitive-20260715/mismatch-ledger.md` passed.
- Full `git diff --check` now passes; the previously noted trailing whitespace in `app/api/webhooks/route.ts` was removed while applying the product-identity webhook copy change.
- Full focused regression command passed: `node --experimental-strip-types --test tests/co-production-copy.test.ts tests/admin-client-front-door.test.ts tests/enterprise-identity-model.test.ts tests/cockpit-review-timeline.test.ts tests/co-production-brand.test.ts tests/review-player-policy.test.ts tests/cockpit-control-regressions.test.ts tests/visual-shell-navigation.test.ts tests/journey-upload-sharing.test.ts tests/auth-policy.test.ts tests/exterior-states.test.ts tests/cockpit-popovers.test.ts` (67 tests passed).
- Expanded focused regression command passed after activity and library work: `node --experimental-strip-types --test tests/co-production-copy.test.ts tests/admin-client-front-door.test.ts tests/enterprise-identity-model.test.ts tests/cockpit-review-timeline.test.ts tests/co-production-brand.test.ts tests/review-player-policy.test.ts tests/cockpit-control-regressions.test.ts tests/visual-shell-navigation.test.ts tests/journey-upload-sharing.test.ts tests/auth-policy.test.ts tests/exterior-states.test.ts tests/cockpit-popovers.test.ts tests/settings-product-polish.test.ts tests/settings-route.test.ts tests/journey-demo-review.test.ts tests/library-product-polish.test.ts tests/activity-product-polish.test.ts` (82 tests passed).
- Expanded focused regression command passed after review-links and project-intake work: `node --experimental-strip-types --test tests/co-production-copy.test.ts tests/admin-client-front-door.test.ts tests/enterprise-identity-model.test.ts tests/cockpit-review-timeline.test.ts tests/co-production-brand.test.ts tests/review-player-policy.test.ts tests/cockpit-control-regressions.test.ts tests/visual-shell-navigation.test.ts tests/journey-upload-sharing.test.ts tests/auth-policy.test.ts tests/exterior-states.test.ts tests/cockpit-popovers.test.ts tests/settings-product-polish.test.ts tests/settings-route.test.ts tests/journey-demo-review.test.ts tests/library-product-polish.test.ts tests/activity-product-polish.test.ts tests/reviews-product-polish.test.ts tests/project-intake-polish.test.ts` (86 tests passed).
- Expanded focused regression command passed after projects workspace work: `node --experimental-strip-types --test tests/co-production-copy.test.ts tests/admin-client-front-door.test.ts tests/enterprise-identity-model.test.ts tests/cockpit-review-timeline.test.ts tests/co-production-brand.test.ts tests/review-player-policy.test.ts tests/cockpit-control-regressions.test.ts tests/visual-shell-navigation.test.ts tests/journey-upload-sharing.test.ts tests/auth-policy.test.ts tests/exterior-states.test.ts tests/cockpit-popovers.test.ts tests/settings-product-polish.test.ts tests/settings-route.test.ts tests/journey-demo-review.test.ts tests/library-product-polish.test.ts tests/activity-product-polish.test.ts tests/reviews-product-polish.test.ts tests/project-intake-polish.test.ts tests/projects-workspace-polish.test.ts` (89 tests passed).
- Expanded focused regression command passed after retention workspace work: `node --experimental-strip-types --test tests/co-production-copy.test.ts tests/admin-client-front-door.test.ts tests/enterprise-identity-model.test.ts tests/cockpit-review-timeline.test.ts tests/co-production-brand.test.ts tests/review-player-policy.test.ts tests/cockpit-control-regressions.test.ts tests/visual-shell-navigation.test.ts tests/journey-upload-sharing.test.ts tests/auth-policy.test.ts tests/exterior-states.test.ts tests/cockpit-popovers.test.ts tests/settings-product-polish.test.ts tests/settings-route.test.ts tests/journey-demo-review.test.ts tests/library-product-polish.test.ts tests/activity-product-polish.test.ts tests/reviews-product-polish.test.ts tests/project-intake-polish.test.ts tests/projects-workspace-polish.test.ts tests/retention-workspace-polish.test.ts` (91 tests passed).
- Expanded focused regression command passed again after the auth access-readiness pass: `node --experimental-strip-types --test tests/co-production-copy.test.ts tests/admin-client-front-door.test.ts tests/enterprise-identity-model.test.ts tests/cockpit-review-timeline.test.ts tests/co-production-brand.test.ts tests/review-player-policy.test.ts tests/cockpit-control-regressions.test.ts tests/visual-shell-navigation.test.ts tests/journey-upload-sharing.test.ts tests/auth-policy.test.ts tests/exterior-states.test.ts tests/cockpit-popovers.test.ts tests/settings-product-polish.test.ts tests/settings-route.test.ts tests/journey-demo-review.test.ts tests/library-product-polish.test.ts tests/activity-product-polish.test.ts tests/reviews-product-polish.test.ts tests/project-intake-polish.test.ts tests/projects-workspace-polish.test.ts tests/retention-workspace-polish.test.ts` (91 tests passed).
- Expanded focused regression command passed after the invite and global-error exterior passes: `node --experimental-strip-types --test tests/co-production-copy.test.ts tests/admin-client-front-door.test.ts tests/enterprise-identity-model.test.ts tests/cockpit-review-timeline.test.ts tests/co-production-brand.test.ts tests/review-player-policy.test.ts tests/cockpit-control-regressions.test.ts tests/visual-shell-navigation.test.ts tests/journey-upload-sharing.test.ts tests/auth-policy.test.ts tests/exterior-states.test.ts tests/cockpit-popovers.test.ts tests/settings-product-polish.test.ts tests/settings-route.test.ts tests/journey-demo-review.test.ts tests/library-product-polish.test.ts tests/activity-product-polish.test.ts tests/reviews-product-polish.test.ts tests/project-intake-polish.test.ts tests/projects-workspace-polish.test.ts tests/retention-workspace-polish.test.ts tests/team-invite-polish.test.ts` (93 tests passed).
- `npm run typecheck` passed.
- Live preview route smokes on `4113` return `200 OK` for `/projects?demo=1`, `/projects/new?demo=1`, `/projects/archive?demo=1`, `/projects/trash?demo=1`, and `/projects/bp?asset=bp-rodeo-v2&view=review&demo=1`.
- Live preview route smokes on `4113` return `200 OK` for `/login?demo=1&next=/projects/bp%3Fasset%3Dbp-rodeo-v2%26view%3Dreview` and `/signup?demo=1&next=/projects/bp%3Fasset%3Dbp-rodeo-v2%26view%3Dreview`.
- Live invite route smoke on `4113` returns the expected unauthenticated redirect: `/invite/demo-token` -> `307`.
- Live preview route smokes on `4113` return `200 OK` for projects, review, reviews, project intake, activity, library, and notification settings.
- Live preview HTML on `4113` includes the supplied raster lockup preload, `Review timeline`, and `Demo waveform queued`.
- `node --experimental-strip-types --test tests/settings-product-polish.test.ts tests/settings-route.test.ts tests/cockpit-control-regressions.test.ts` passed after the source-operations Systems pass (20 tests passed).
- `npm run typecheck` passed after the source-operations Systems pass.
- Path-scoped `git diff --check -- components/auth/StudioSystemsSettings.tsx components/auth/SettingsPillar.module.css tests/settings-product-polish.test.ts docs/design-evidence/co-videopro-definitive-20260715/mismatch-ledger.md` passed after the source-operations Systems pass.
- Trailing-whitespace scan passed for the source-operations touched paths.
- Live route smoke after the source-operations Systems pass: `curl -I http://localhost:4113/settings?section=systems&demo=1` returns `200 OK`; the Systems panel is client-rendered, so static HTML does not prove visual acceptance.
- `node --experimental-strip-types --test tests/settings-product-polish.test.ts tests/settings-route.test.ts tests/cockpit-control-regressions.test.ts` passed after the source-operations honesty correction (20 tests passed).
- `npm run typecheck` passed after the source-operations honesty correction.
- Path-scoped `git diff --check -- components/auth/StudioSystemsSettings.tsx components/auth/SettingsPillar.module.css tests/settings-product-polish.test.ts docs/design-evidence/co-videopro-definitive-20260715/mismatch-ledger.md` passed after the source-operations honesty correction.
- Trailing-whitespace scan passed for the source-operations honesty correction touched paths.
- Live route smoke after the source-operations honesty correction: `curl -I http://localhost:4113/settings?section=systems&demo=1` returns `200 OK`; `curl http://localhost:4113/api/transcode/worker` returns `503 AUTH_NOT_CONFIGURED`, matching the UI copy that the worker endpoint is machine-only and not proof of worker readiness.
- `node --experimental-strip-types --test tests/cockpit-control-regressions.test.ts tests/review-player-policy.test.ts` passed after the annotation-coordinate pass (19 tests passed).
- `npm run typecheck` passed after the annotation-coordinate pass.
- Live route smoke after the annotation-coordinate pass: both `4113` and untouched `4103` return `200 OK` for `/projects/bp?asset=bp-rodeo-v2&view=review&demo=1`; `4113` is listening on port 4113 and continues to preload the supplied Co-Production Pro raster.
- `node --experimental-strip-types --test tests/exterior-states.test.ts tests/cockpit-control-regressions.test.ts tests/co-production-brand.test.ts` passed after the exterior-brand pass (22 tests passed).
- `npm run typecheck` passed after the exterior-brand pass.
- Live route smokes after the exterior-brand pass: `4113` and untouched `4103` return `200 OK` for the review route; `4113` login returns `200 OK` and preloads both product marks; `4113` missing route returns `404 Not Found` and preloads `/brand/co-production-pro-stacked.png`.
- `node --experimental-strip-types --test tests/cockpit-control-regressions.test.ts tests/journey-upload-sharing.test.ts` passed after the upload terminal-state pass (25 tests passed).
- `npm run typecheck` passed after the upload terminal-state pass.
- Path-scoped `git diff --check` and trailing-whitespace scan passed for `app/(dashboard)/projects/[id]/page.tsx`, `components/projects/ProjectCockpit.tsx`, `app/globals.css`, `tests/cockpit-control-regressions.test.ts`, and this ledger after the upload terminal-state pass.
- Live route smokes after the upload terminal-state pass: `4113` returns `200 OK` for `/projects/bp?asset=bp-rodeo-v2&view=review&demo=1` and `surface=versions`; untouched `4103` returns `200 OK` for the review route.
- `node --experimental-strip-types --test tests/co-production-copy.test.ts` passed after the health-identity pass (4 tests passed).
- `npm run typecheck` passed after the health-identity pass.
- Health route smokes after the health-identity pass: `4113` `/api/health` returns `{"service":"co-deliver","product":"Co-Production Pro","brand":"Content Co-op","port":4113}`; `/api/health/live` returns the same display identity; `/api/health/ready` and `/api/health/dependencies` return the expected `503 Service Unavailable` with database and storage failures plus the same display identity.
- `node --experimental-strip-types --test tests/cockpit-control-regressions.test.ts tests/cockpit-review-timeline.test.ts tests/cockpit-toolbar-responsive.test.ts` passed after the latest review-density pass (30 tests passed).
- `node --experimental-strip-types --test tests/settings-product-polish.test.ts tests/settings-route.test.ts tests/exterior-states.test.ts` passed after the latest Systems telemetry/mobile pass (8 tests passed).
- `npm run typecheck` passed after the latest review-density and Systems telemetry/mobile passes.
- Live route smokes after the latest pass: `4113` and untouched `4103` return `200 OK` for `/projects/bp?asset=bp-rodeo-v2&view=review&demo=1`; `4113` returns `200 OK` for `/settings?section=systems&demo=1`.
- Tracked `git diff --check -- app/globals.css tests/cockpit-control-regressions.test.ts` passed, and trailing-whitespace scan passed for `app/globals.css`, `components/auth/StudioSystemsSettings.tsx`, `components/auth/SettingsPillar.module.css`, `tests/cockpit-control-regressions.test.ts`, `tests/settings-product-polish.test.ts`, and this ledger.
- `node --experimental-strip-types --test tests/settings-product-polish.test.ts tests/settings-route.test.ts tests/exterior-states.test.ts` passed again after the managed-notification settings pass (8 tests passed).
- `npm run typecheck` passed after the managed-notification settings pass.
- Notification route smokes after the managed-notification pass: `4113` returns `200 OK` for `/settings?section=notifications&demo=1`; unauthenticated non-demo `/settings?section=notifications` redirects to `/login?next=...&demo=1`.
- Path-scoped `git diff --check -- app/globals.css 'app/(dashboard)/settings/page.tsx' tests/cockpit-control-regressions.test.ts tests/settings-product-polish.test.ts` passed, and trailing-whitespace scan passed for the currently touched files.
- `node --experimental-strip-types --test tests/exterior-states.test.ts tests/settings-product-polish.test.ts tests/settings-route.test.ts tests/cockpit-control-regressions.test.ts tests/cockpit-review-timeline.test.ts tests/cockpit-toolbar-responsive.test.ts` passed after the demo-session exterior pass (38 tests passed).
- `npm run typecheck` passed after the demo-session exterior pass.
- Live route smokes after the demo-session exterior pass: `4113` returns `200 OK` for the review route and `/login?demo=1`.
- Path-scoped `git diff --check` and trailing-whitespace scan passed for the currently touched cockpit, settings, demo-session, test, and ledger files.

## Next Loop

- Capture matched desktop and `390x844` mobile screenshots once the chosen in-app browser is available, then compare against references 1, 2, 3, 8, and 10 side by side.
- Inspect upload, share, notifications, account, settings/preferences, lifecycle drawer, public review, and mobile drawers interactively.
- Tighten spacing around review strip, player, timeline, and dock after screenshot evidence. Do not call any surface clean until visible evidence exists.
