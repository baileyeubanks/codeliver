# CCO Design Universe — Adoption Map (mega audit → this build)

**Date:** 2026-07-17 · **Source:** `~/Documents/kimi/workspace/CCO_DESIGN_UNIVERSE_MEGA_AUDIT.md` (16 scopes, cherry-pick registry, dialect doctrine) · **Supersedes:** `COPROVIDEO_NORTHSTAR_UI_PLAN.md` where they conflict · **Governs with:** `COPROVIDEO_DESIGN_BIBLE.md` (atmosphere), `WEBSTER_MISSION_STATE.md` (behavioral gates)

## 1. Dialect law (the war is over)

| Altitude | Dialect | Where it applies here |
|---|---|---|
| Public | Cream Editorial (#f0ebe0 parchment, Fraunces, copper accents) | /welcome, future brief funnel, client documents |
| Working | Royal Light Cockpit (cool gray, white cards, royal blue, dark media islands) | Home, Opportunities, Projects, cockpit shell |
| Stage | Dark Cinema (near-black wells only) | review stage, SequenceTimeline, player surfaces |
| Deep work | Paper Desk (canvas #eae2d3, rust/moss) | future writing surface (Co-Script weave, T6) |
| Terminal | **No surface in Co‑ProVideo** | money surfaces are editorial (Surface F), not Bloomberg — the operative meaning of "delete ROOT" |

Color = state only (law 2). Every number is a door (law 3). Nothing beautiful that lies (epilogue).

## 2. Surface mapping (audit surface → current build → next)

- **A Welcome** → `/welcome` exists (gradient hero) → restyle to Cream Editorial + dawn gradient + serif headline (keep truthful copy).
- **B Auth** → login → adopt the audit's cream/navy split from `cco-videopro-ui-enrichment/app/login/page.tsx` (registry #15 — the other worktree's login is the named winner).
- **C Home** → shipped (media-first, needs-you ledger, latest media) — matches the audit ~90%; add verb-pills per row (Triage/Review/Consolidate/Deliver) + floating "+ New" two-choice menu.
- **D Pipeline** → shipped (inbox, proposals with amounts, monogram clients, adaptive discovery card) — matches the audit; nothing to do.
- **E Brief & Discovery** → discovery exists in-product → the 14-step choreography + live estimate preview is the future *public* funnel (separate surface, later).
- **F Proposal & Estimate** → shipped (versioned card, milestones, compile-from-rate-card) — the audit's pick matches ours; add status-chain header + provenance line (partially present).
- **G Proposal Studio** → NOT adopted as an app (law 10: one money surface). Its live-cover repaint becomes the **document renderer** over our estimate data (Gen-3 dark cover from registry #12/#14).
- **H Project Cockpit** → shipped (the audit describes our cockpit nearly verbatim — lifecycle rail, stage pill, Review/Edit/Focus toggle, Plan/El Paso block, Reviews consolidation + Decision Ledger + outbox).
- **I Script Studio** → future (T6 Co-Script weave): paper-desk costume change, passes panel, preview-first with Replace/Insert/Append/Copy as explicit verbs (audit law 6).
- **J Sequences** → shipped (SequenceTimeline matches the audit's edit bay incl. Split/Delete/EDL + transcript workbench).
- **K Review, internal** → Review Theater landed (parallel session) + audit's transport/keyboard/annotation canon queued for the internal player.
- **L Review, public** → shipped (white-label client page + Finish Review) — the audit's crown jewel matches ours; brand-variable injection (client logo/accent per share) is the remaining piece.
- **M Library** → exists (folders + cards) → add thumbnail-size slider + reviews ledger trust footer.
- **N Delivery & QC** → shipped (spec form + status conveyor) → watermark config + download permission pills next.
- **O Money Console (ROOT)** → **not adopted** — deleted-by-doctrine (see §3).
- **P Documents** → adopt Gen-3 dark cinematic quote cover + invoice one-pager + 9-page proposal rhythm as the document layer over Surface F data (T12-adjacent).
- **Q Settings & Brand Governance** → exists partially (BrandSettings) → honesty pills everywhere (already law here).

## 3. ROOT bits — deletion ledger (pending user confirmations)

| Item | Where | Action | Status |
|---|---|---|---|
| ACS seed/mention content | this repo | purged (prior) | ✅ done |
| ACS content in shared packages / CC client flows | cco-main, packages/* | excise content, keep patterns | ⏸ needs "successor confirmed" |
| "Quote & Proposal Skills 2" (byte clone) | Desktop skills folder | delete-with-ledger | ⏸ needs go |
| Lime `#b9ff77` token litter | 3× packages/brand copies | purge to archive | ⏸ needs go |
| Atlantis light-admin kit | cco-main | archive | ⏸ needs go |
| Stale $1,500/day rate card | Honeybook-era tables | replace with RATES.md (Jul 2026) values | ⏸ needs go |
| Fixture theater (fake toggles/fixtures) | various | honesty pills or make real | ongoing (law already applied here) |

**No live system is in deletion scope.** The ACS product (its own worktree/servers) is untouched forever per the mission boundary.

## 4. Open user decisions

1. "Successor confirmed" for cco-main's ROOT console (Financial Studio + proposal/milestones replaces the CC money surface; ACS remains separate).
2. Fossil deletions (§3 rows 3–6) go-ahead.
3. Paper Desk dialect: adopt for the future writing surface, or defer.

## 5. Naming and colorway — final law (2026-07-17)

- **CCO OS** = the Content Co-op universe as a whole (public site + admin shell + client surfaces).
- **co-videopro** = the admin/operator shell (this repo's product; the MAIN_INSPIRATION surface direction). Client surfaces stay radically simpler. The public contentco-op.com branding is untouched (Cream Editorial — "it's great").
- **CVP four-color identity: kept, MUTED.** The saturated four-color mark is replaced in-app by a muted colorway (`public/brand/cvp-mark-muted.png`, `cvp-stacked-muted.png` — ffmpeg hue=s=0.5: terracotta, slate blue, sage-teal, honey) so identity blends with the graphite/cream aesthetics. Originals preserved on disk; saturated mark may still fly at the door (public marketing) but not inside working surfaces.
