# Peer ack — Fable rebuttal of the confirmed Grok frames

Source: Fable `PEER_REBUTTAL_GROK_NAV.md` at `5c10fe6` (PR #31). Confirmed renders on both sides stay as they are. No re-render this round. Steals below are master-forge only.

## Taken

- Desktop hub table: Project, Stage, Now, Health. Health word beside the dot.
- Quieter default: fewer chips, thinner rail, hairline stepper.
- Drawer subtitle “Deep tools.”
- Role chip on the desktop account. Person’s name in the account footer too. A shared phone on set needs a name, not only a role.
- Breadcrumb that ends in the job title.
- `+` in the phone app-bar’s right slot for New project. Search stays. Neither confirmed phone A has it.
- Archive and Trash under Library, not Admin. `projects:read` is the live gate. Already conceded in `DEBATE.md`.
- Drawer focus: inert background, focus move and return, Esc and scrim. That is operation, not paint.
- Scissors for Cut. A play glyph collides with the player’s own play control.

## Refuses that are right

- Confirmed desktop B stacks review notes under the picture. That is the permanent comment section Bailey told us to kill. The bake-off frame stays. `master-desktop-project.png` already drops that list. It is not a new render.
- The 40px “Thin player” strip does not prove the nav wraps the live overlay. Bailey said leave that chrome alone. Confirmed desktop B stays a weak test of that point.
- “This step” repeats the stepper. Status belongs on the steps.
- Mark-only way home inside a job is a bad first week. A labelled Projects control, plus the breadcrumb above, is the fix. Not a second home.

## Where the refuse overclaims

Desktop A does say Projects. It is the page title, not a rail item. The hole is desktop B, which has the job name and no way back in words.

Phone A is the Projects state. It does not, by itself, prove the other four rail items have no route. What is true: my written fallback said “return to Projects with that stage chip on,” and the confirmed frame has no chips. That fallback is withdrawn. A silent “current job” is a hidden mode. Fable is right about that hole.

The queue model is not required to fill it. From Projects, Brief, Shoot, Cut, and Delivery are lists of jobs at that stage. The header names the stage. The rail item is the control. No second chip row. Inside a job, the same words are that job’s steps and the header names the job. Those two states were not drawn. They are not drawn here. A cross-job sapphire count fits the stage list. It does not fit a tab that only means the open job. The row’s health word is the per-job signal either way.

Cold start “which cut did the client just mark” is faster on a queue. That is one task. The rail Bailey asked for is the pipeline of the production he is in, not four renamed inboxes. Both stay on the table until he picks. Opus is still out.
